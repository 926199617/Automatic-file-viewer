## 技能名称
自动文件查看器

## 描述
当用户发送文件/图片（截图、附件、文档等）且AI无法直接查看内容时，自动到指定路径查找并读取。

## 功能
- 自动定位用户发送的文件
- 支持读取图片（后端识图模型自动分析）
- 支持读取文本/代码文件
- 大文件分段读取
- 基于实际内容准确回答

## 使用方法
将 `自动文件查看器.skill` 文件放入技能目录，Operit 会自动识别，然后在「包管理 → 技能」中开启开关即可。
